name: Build and Test Stock Portfolio Allocation

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 24
        uses: actions/setup-java@v3
        with:
          java-version: '24'
          distribution: 'temurin'

      - name: Run Java Linter Only
        uses: super-linter/super-linter@v8.0.0
        env:
          DEFAULT_BRANCH: develop
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_JAVA: true
          JAVA_CHECKSTYLE_SUPPRESSIONS_FILE: .github/checkstyle/suppressions.xml
          FILTER_REGEX_EXCLUDE: .*[/\\]src[/\\]test[/\\].*
          DISABLE_GITHUB_STATUS_API: true
          VALIDATE_GIT_COMMIT_HISTORY: false
          HTTP_TIMEOUT: 30

      - name: Setup ReviewDog
        uses: reviewdog/action-setup@d8edfce3dd5e1ec6978745e801f9c50b5ef80252
        with:
          reviewdog_version: latest

      - name: Build the project (compile without tests)
        run: mvn clean compile -DskipTests

      - name: Run tests
        run: mvn test
